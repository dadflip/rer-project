# Local deployment-specific variables (do NOT commit secrets in real projects)
# Copy this file to `postgres.local.env` and edit local-only values.

# User/Group for mounted volumes
PUID=1000
PGID=1000

# Timezone
TZ=Europe/Paris

# PostgreSQL credentials (can be customized per-deployment)
POSTGRES_DB=rerdb
POSTGRES_USER=reruser
POSTGRES_PASSWORD=rerpass
POSTGRES_PORT=5432

# Any variables that should NOT be generated per-university can go here.
